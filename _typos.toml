[default.extend-words]
# Don't correct the acronym ot for overtime
ot = "ot"

[default]
extend-ignore-identifiers-re = [
    # Don't correct the name MAKARxJESUS
    "MAKAR.*",
]

[type.golangcilint]
extend-glob = [".golangci.yaml"]
check-file = false
